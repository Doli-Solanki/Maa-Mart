# Grocery Store - Full Stack Scaffold

Structure:
- backend/  -> Node + Express + Sequelize (MySQL)
- frontend/ -> React (Vite)

Defaults:
- JavaScript (not TypeScript)
- Payment: Razorpay TEST placeholder + Cash on Delivery

Next steps:
1. Fill backend .env with MySQL credentials
2. npm install in both backend and frontend
3. Start backend (npm run dev) and frontend (npm run dev)
4. Seed products: POST http://localhost:5000/api/products/seed

# Backend - Grocery Store (Node + Express + MySQL + Sequelize)

Steps to run:

1. Copy `.env.example` to `.env` and fill DB credentials.
2. Install dependencies:
   ```
   npm install
   ```
3. Start server:
   ```
   npm run dev
   ```
4. Seed products:
   POST /api/products/seed
